<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# PIC16F877A LED Blink

Hardware smoke example for a LED on `PORTB0`.

Commands:

```bash
make
make size
make flash FLASH_CMD="your-programmer-command"
```

Expected behavior: `RB0` toggles. Configure oscillator and board wiring for your hardware.
